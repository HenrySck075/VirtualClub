{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Avalonia: VirtualClub",
            "type": "dotnet",
            "request": "launch",
            "projectPath": "${workspaceFolder}/VirtualClub.csproj"
        }
    ]
}