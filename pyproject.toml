[project]
name = "virtual-club"
version = "0.1.0"
description = "g"
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
    "pyside6-fluent-widgets[full]>=1.11.2",
]

[dependency-groups]
dev = [
    "pybind11>=3.0.4",
    "pyinstaller>=6.21.0",
]
